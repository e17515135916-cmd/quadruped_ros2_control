# Core dependencies
pyyaml>=6.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
pillow>=10.0.0

# ROS2 dependencies (installed via rosdep)
# rclpy
# sensor_msgs
# geometry_msgs
# std_msgs

# URDF parsing
# urdfpy>=0.0.22  # Optional, can use xml.etree instead

# Visualization
graphviz>=0.20.0

# Screenshot capabilities (Linux)
python-xlib>=0.33; sys_platform == 'linux'
pyautogui>=0.9.54

# Scientific computing
scipy>=1.10.0

# Testing
pytest>=7.4.0
hypothesis>=6.82.0
